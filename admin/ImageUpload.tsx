"use client";

export default function ImageUpload() {
  return (
    <div
      style={{
        border: "1px dashed #333",
        padding: "32px",
        borderRadius: "20px",
      }}
    >
      <input type="file" />
    </div>
  );
}